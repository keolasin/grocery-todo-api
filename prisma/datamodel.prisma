type Food {
    id: ID! @id
    createdAt: DateTime! @createdAt
    name: String!
    quantity: Int!
    inCart: Boolean!
    postedBy: User
}

type User {
    id: ID! @id
    name: String!
    email: String! @unique
    password: String!
    foods: [Food!]!
}