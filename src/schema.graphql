type Query {
    info: String!
    feed(filter: String, skip: Int, first: Int): [Food!]!
    food(id: ID!): Food
    groupList(): [Group!]!
}

type Mutation {
    post(name: String!, quantity: Int!, inCart: Boolean!): Food!
    updateFood(id: ID!, name: String!, quantity: Int!, inCart: Boolean!): Food
    deleteFood(id: ID!): Food

    signup(email: String!, password: String!, name: String!): AuthPayload
    login(email: String!, password: String!): AuthPayload

    vote(foodId: ID!): Vote

    createGroup(name: String!, isPrivate: Boolean!): Group!
    updateGroup(id: ID!, name: String!, isPrivate: Boolean!): Group
    deleteGroup(id: ID!): Group
}

type Food {
    id: ID!
    name: String!
    quantity: Int!
    inCart: Boolean!
    postedBy: User
    votes: [Vote!]!
}

type AuthPayload {
    token: String
    user: User
}

type User {
    id: ID!
    name: String!
    email: String!
    foods: [Food!]!
    groups: [Group!]!
}

type Subscription {
    newFood: Food
    newVote: Vote
    newGroup: Group
}

type Vote {
    id: ID!
    food: Food!
    user: User!
}

type Group {
    id: ID!
    name: String!
    users: [User!]!
    createdBy: User!
    isPrivate: Boolean!
}